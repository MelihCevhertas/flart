name: flart_executor
description: Sandbox executor for flart — runs scripts in dart/bash/python/node and captures bounded output.
publish_to: none
version: 0.3.0-dev

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  flart_core:
    path: ../flart_core
  meta: ^1.15.0
  path: ^1.9.0
