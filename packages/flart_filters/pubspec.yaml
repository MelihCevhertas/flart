name: flart_filters
description: Reactive filters for flart — compact summaries of flutter/dart command output.
publish_to: none
version: 0.1.0-dev

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  flart_core:
    path: ../flart_core
  meta: ^1.15.0
  path: ^1.9.0
  yaml: ^3.1.2
